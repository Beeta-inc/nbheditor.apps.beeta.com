package com.beeta.nbheditor

/**
 * Identical to MainActivity but launched with Theme.Nbheditor.Glass
 * declared in the manifest — this is the only reliable way to set
 * windowIsTranslucent=true before the window is created.
 */
class GlassMainActivity : MainActivity()
